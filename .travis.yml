language: java

notifications:
  email:
      recipients:
        - hubert.lindenau@gmail.com
      on_success: never
      on_failure: always
    
jdk: openjdk15 
   
before_deploy:
  - mvn install
  - mvn test -B

deploy:
  provider: releases
  overwrite: true
  skip_cleanup: true
  edge: true
  token: $GITHUB_OAUTH
  on:
    branch: working
  file:
    - target/bootstrap-project-architecture-$TRAVIS_TAG.jar
